# TEST_SPEC.md — japan-weather-atmosphere-atlas

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- `integration` は取得済みキャッシュ(`data/cache/`)が在るときだけ走る。無いときは skip し、
  **skip を成功と読み替えない**(件数を完了報告に書く)
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す

## 期待値の出所(HC-016)

| 出所 | 本プロジェクトでの適用 |
|---|---|
| SPEC の条項 | SPEC の §番号 / G-xx を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威 | 気象庁の公式凡例・公表統計値。フィクスチャ先頭に URL と取得日を書く |
| 実測 | 実測日と実測値、**そして母集団**(何を何件走査したか)をコメントに残す |

件数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
ただし外部権威の公表値との一致は、公表値そのものが期待値なので定数でよい(出所を明記する)。

## フィクスチャと権利

| フィクスチャ | 出所 | 同梱の可否 |
|---|---|---|
| `tests/fixtures/jma/daily_tokyo_2023_{01..12}.html` | 気象庁 過去の気象データ検索(2026-09-08 取得) | 同梱可(公共データ利用規約・出典明記) |
| `tests/fixtures/jma/monthly_tokyo_2023.html` | 同上 | 同梱可 |
| `tests/fixtures/jma/pref44.html` | 気象庁 地点選択ページ | 同梱可 |
| `tests/fixtures/jma/co2_yearave.html` | 気象庁 CO2 年平均値 | 同梱可 |
| `tests/fixtures/jma/ghg_monthly/*.co2 / *.ch4` | 気象庁 大気・海洋環境観測年報 | 同梱可 |
| NIES の生ファイル | 国立環境研究所 | **同梱しない**(SPEC §2.5)。合成フィクスチャ + キャッシュ有り時の integration |

NIES は合成フィクスチャを使う。**合成物が主張したい性質を実際に持つことをテスト内で assert する**
(HC-068 — 自作物のほうが「知っているつもり」になって観測が飛ぶ)。

## オラクルの出所

| オラクル | 出所 | 性格 | 循環しないと言える理由 |
|---|---|---|---|
| CO2 年平均 | 気象庁が別ページで公表する年平均値(1987–2025) | 外部権威 | 気象庁は年平均を独自の手順で算出しており、我々が月別ファイルから計算する経路とは独立 |
| 月合計降水量 / 月平均気温 | 気象庁が公表する「月ごとの値」 | 外部権威 | 日別ページと月別ページは別々に公表されている |
| 記号の意味 | 気象庁公式凡例 `mdrr/man/remark.html` | 外部権威 | 我々の解釈を凡例側が使っていない |
| 80% 基準 | 同凡例の「準正常値」の定義 | 外部権威 | `--`=欠測 説を否定するのに使う。仮説を検査側で使っていない |
| 測定局座標 | 都道府県の外接矩形・霞が関の既知座標 | 外部知識 | TM ファイル自身の数字を基準に使っていない |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | G-01 | `--` を降水量として読む | 欠測ではなく 0.0、品質 `no_phenomenon` |
| T-002 | G-01 | `0.0` を降水量として読む | 0.0(微量)、品質 `valid`。T-001 と**区別できる** |
| T-003 | G-01 | 公式凡例の全記号 `-- / 0.0 / ) / ] / × / /// / 空白 / #` の対照表 | SPEC §2.1 の表と一致 |
| T-004 | G-01 | **陽性対照**: `--`=欠測 と解釈して東京 2023 の月別被覆率を出す | 80% を割る月が存在する(= その解釈は公表月値に `)` `]` が付かないことと矛盾) |
| T-005 | G-01 | **陰性対照**: `--`=0 と解釈した場合の被覆率 | 全 12 か月で 100% |
| T-006 | G-02 | 表 `tablefix1` を含まない応答を読む | 例外で停止(黙って空を返さない) |
| T-007 | G-02 | 日付行数が当月日数と一致しない応答を読む | 例外で停止 |
| T-008 | G-02 | 正常な応答を読む | 当月日数と同じ行数、日は 1..日数の連続 |
| T-009 | G-05, F-02 | 東京 2023 の日別降水量を月ごとに合計 | 公表の月合計と 12/12 一致(気象庁公表値) |
| T-010 | G-05, F-02 | 東京 2023 の日別平均気温を月ごとに平均 | 公表の月平均と 12/12 一致(丸め 0.1 の範囲) |
| T-011 | G-03 | 地点マスタの見出し行が想定と違うページを読む | 例外で停止(列の意味を推測しない) |
| T-012 | G-03 | `pref44.html` から地点を抽出 | 東京(`s`, 47662)が在り、緯度経度・標高・要素フラグが取れる |
| T-013 | G-03 | 富士山 `47639` を 2 つの prec から読む | `block_no` で 1 件に名寄せされ、座標が一致する |
| T-014 | G-06 | GHG 月別ファイルの全行でセンチネルとフラグを突き合わせる | 不一致 0 件(6 系列すべて) |
| T-015 | G-06 | 月別ファイルの `DD` 欄 | すべて 99(月別値の約束) |
| T-016 | G-04 | CO2 月別 → 年平均 を公表年平均と突き合わせる | 12 か月そろう年はすべて公表の丸め(0.1 ppm)で一致 |
| T-017 | G-04 | 公表表の `*` 印が付く年 | 有効月数 < 12 である |
| T-018 | F-07 | GHG 系列の時刻が単調増加で重複が無い | 各観測点・各物質で成立 |
| T-019 | G-07 | NIES 測定局の度分秒 → 十進 | 秒に小数を含む行も正しく変換される |
| T-020 | G-07 | 合成の測定局マスタ(性質を assert したうえで) | 県の外接矩形内に入る / 矩形外は検出される |
| T-021 | F-06 | NIES 時間値 → 日別値 | `9999` は欠測、被覆が閾値未満の日は欠測 |
| T-022 | F-06 | **陽性対照**: `9999` を数値として扱う | 日平均が現実的な上限を超える(= その扱いは誤り) |
| T-023 | F-06 | 年度(4 月始まり)から暦日への変換 | FY2023 は 2023-04-01 .. 2024-03-31 |
| T-024 | G-15 | 日本語の本文・ソースに別字種・制御文字が無い | `harness/text_hygiene.py` が 0 件 |
| T-025 | SPEC §9 | SPEC のゲート表の各 `G-xx` が、テストから参照されるか「未実装」と明記されている | どちらでもない `G-xx` があれば落とす |
| T-026 | G-17 | 国内の矩形で絞る。昭和基地は外・国内の実在地点は内。絞った集合の四隅が既知の極地点と一致 | 陽性/陰性の対照つき |
| T-027 | G-02 | 応答の見出しが、要求した地点・年月と一致する | 別の月・別の地点は例外(陽性対照) |
| T-028 | G-18 | 暦に無い日(4/31 等)の行は日値にしない。9998/9999 の出る場所が入れ替わったら例外 | 陽性対照つき |
| T-029 | G-18 | PM2.5 は PM25 / PMFL / PMBH の 3 コードで入る。単位が書式説明書と違えば例外 | 書式説明書 TJ_manu.pdf |
| T-030 | G-08 | 配信した埋め込み由来の資産に NaN が無く、値域が仕様どおり | 実データ |
| T-031 | G-09 | 類似日に自分自身が入らない。すべての日で成立 | 実データ |
| T-032 | G-10 | 異常度が 0..100 に収まり、日付の集合が埋め込みの日付と一致 | 実データ |
| T-033 | G-11 | model_metadata の学習・検証・試験の期間が時系列順で重ならない | 実データ |
| T-034 | G-19 | 異常度の中央値が学習期間と held-out で 10 点以内 | 実データ・HC-251 の再発防止 |
| T-035 | HC-152 | SPEC / MODEL_CARD に書いた数(損失・異常度の段差・シルエット・配信物の大きさ・地点数)が出荷物と一致する | 出荷物から取り直して突き合わせる |
| T-036 | N-04 | `.vercelignore` は先頭 `/` で固定し、キャッシュ・venv・node_modules・作業途中の出力を除外し、画面が要るものは残す | 無印 `data/` が public/data を消す陽性対照つき |
| T-037 | N-01 | ビルド刻印は決定的・改行に依存しない・画面ソースとデータの変更で変わる・配らないものでは変わらない | 陽性対照つき(HC-148) |
| T-038 | N-01 | 画面のコードは配られない経路(data/cache 等)を読まない | 陽性対照つき |
| T-039 | N-04 | `.gitignore` が作業途中の出力に当たり、data/processed が追跡されていない。フッタの宛先が本物 | 実測で 785 ファイルが追跡されていた |
| T-040 | HC-271 | フッタ 5 項目の行き先を項目ごとに照合する。歩き方・設計図は `claude.ai/code/artifact/<小文字 UUID>` で互いに異なる | 陽性対照つき(README / SPEC の暫定リンク・崩れた ID を拒む)。本番は `smoke_prod.mjs` が DOM の href で同じ形を見る |
| T-041 | N-05 | `npm run verify` のチェーンを展開し、対象の無いテスト段が無く、pytest と実ブラウザ検品を含み、ビルドが検品より前 | loop_000 から `vitest run`(テストファイル 0 本)で exit 1 のまま止まっていた。陽性対照つき |

## 未実装のゲート(SPEC §9 と対応)

実装済み: `G-01`〜`G-11`, `G-15`, `G-17`〜`G-19`。
未実装として SPEC に明記してあるのは `G-12`(配信 JSON に生ファイルが混入していない)・
`G-13`(欠測を 0 と表示しない・UI 側の機械検査)・`G-14`(出典への到達・UI 側)・
`G-16`(図が `viewBox` に収まる)。

ただし `G-13` `G-14` `G-16` は**実ブラウザ検品 `harness/smoke.mjs` が実際に見ている**
(欠測の凡例・出典への到達・SVG の文字の重なりと `viewBox`)。pytest から参照されないので
SPEC 側は「未実装」のままにしてあるが、無検査ではない。T-025 はこの対応を機械で数える。
