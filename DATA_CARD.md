# DATA_CARD

本アトラスが使う各データ源について、出典・取得・加工・品質・既知の問題を記録する。
数値はすべて実測で、実測日と母集団を添える。

---

## 1. 気象庁 地上気象観測 日別値(JMA-WX)

| 項目 | 内容 |
|---|---|
| 出典 | 気象庁ホームページ「過去の気象データ検索」 |
| URL | `https://www.data.jma.go.jp/stats/etrn/view/daily_s1.php` |
| 取得日 | 2026-09-09 以降 |
| ライセンス | 公共データ利用規約(第 1.0 版)。出典記載と加工の明示が必要 |
| raw file | 1 地点 1 か月の HTML。`data/cache/jma/` に保存(リポジトリには入れない) |
| transform | 表 `tablefix1` を切り出し、記号を品質に直し、long format の JSONL へ |
| 再配布 | 派生値のみ(`public/data/weather/YYYY-MM.json`) |

### 実測

- 構想書の `/obd/stats/etrn/` は **301 で `/stats/etrn/` へ移動**していた。
- 応答時間は中央値 0.161 s / 平均 0.224 s / 最大 0.474 s(n=6)。
  正常な月ページは 36,846〜37,996 バイト。
- **不整合な `prec_no`/`block_no` でも HTTP 200 と 3,814 バイトの短いページが返る**。
  取得器は表の存在・日付行数・見出しの地点名と年月を検算してから採る。

### 値欄の記号(公式凡例 `stats/data/mdrr/man/remark.html`)

`--` = 該当現象がない(降水量では 0 mm という**観測値**) / `0.0` = 量はあるが 0.5 mm 未満 /
`)` = 準正常値(80% 基準) / `]` = 資料不足値(値を採らない) /
`×` `///` 空白 = 欠測 / `#` = 疑問値。

**`--` を欠測として扱うと、雨の降らなかった日がすべて消える。**
東京 2023 では 365 日中 189 日が `--` で、欠測とみなすと 1 月の被覆が 11/31 = 35% になる。
公表の月別値には資料不足の記号が 1 つも付いていないので、その解釈は公表値と矛盾する。

### known issues

- 富士山(`block_no` 47639)は県境の山頂で、`prec_no` 49(山梨)と 50(静岡)の
  両方に現れる。`block_no` で名寄せする。
- **「官署」は国内に限らない。** 現役官署 156 件に昭和基地(南緯 68.995 度)が含まれる。
  国内の矩形で絞って 155 件にする。
- 富士山の**現地気圧は 620 hPa 台**になる。標高 3,775 m の観測点なので誤りではない。
  範囲の検査は標高から標準大気の気圧を求めて当てる。

---

## 2. 気象庁 観測地点マスタ(JMA-STN)

| 項目 | 内容 |
|---|---|
| 出典 | 気象庁「過去の気象データ検索」地点選択ページ |
| URL | `https://www.data.jma.go.jp/stats/etrn/select/prefecture.php?prec_no=NN` |
| 取得日 | 2026-09-09 |
| transform | ページ内の `viewPoint()` 引数を読む。**先頭の 1 件が列名の見出し行** |

### 実測(61 の `prec_no` を全走査)

| 母集団 | 地点 | 官署 | 現役官署 |
|---|---:|---:|---:|
| `prec_no` と `bk_no` の対 | 1,679 | 160 | 157 |
| `block_no` で名寄せ後 | 1,678 | 159 | 156 |
| 国内の矩形で絞った後 | — | — | **155** |

アメダス 1,519、終了地点 391、備考つき 108(対の母集団)。
絞った 155 件の四隅は独立に知られた極地点と一致する:
東端・南端 南鳥島(24.288N / 153.983E)、西端 与那国島(123.010E)、北端 稚内(45.415N)。

都道府県は `prec_no`(予報区・61 種)からは決まらないので、
**座標から国土地理院の逆ジオコーダで決めた**。155 件中 148 件はそのまま解決し、
残り 7 件(富士山・呉・厳原・佐世保・油津・宇和島・清水)は座標が海上や
自治体界の外に落ちるため、1 km 以内の近傍を探して解決した。47 都道府県すべてに官署がある。

---

## 3. 気象庁 温室効果ガス(JMA-GHG)

| 項目 | 内容 |
|---|---|
| 出典 | 気象庁「大気・海洋環境観測年報」 |
| URL | `https://www.data.jma.go.jp/env/data/report/data/download/atm_bg/{species}.zip` |
| 取得日 | 2026-09-09 |
| raw file | co2.zip 11,215,507 B / ch4.zip 9,056,982 B |
| transform | 月別の固定長を読み、`F != 3` を欠測として除く |
| 再配布 | 月別値と年平均(`public/data/ghg/monthly.json`) |

### 実測

観測点は 3 つ(綾里 `ry` / 南鳥島 `mi` / 与那国島 `yo`)。

| 物質 | 綾里 | 南鳥島 | 与那国島 |
|---|---|---|---|
| CO₂ | 1987-01..2023-12(444 行 / 有効 443) | 1993-01..(372 / 369) | 1997-01..(324 / 323) |
| CH₄ | 1991-01..2023-12(396 / 358) | 1994-01..(360 / 357) | 1998-01..(312 / 310) |

- 欠測センチネル `99999.99` と品質フラグ `F != 3` は**全 2,208 行で完全に一致**した。
- 添付 readme は **UTF-8**(CP932 ではない)。
- 2023 年に標準ガスのスケールを統一し、観測開始以降の値が最大 +0.3 ppm 程度修正された。

### オラクル

気象庁が別ページで公表する CO₂ 年平均(`co2_yearave.html`、1987–2025)と、
配布 ZIP の月別値から自前で計算した年平均を突き合わせた。
**12 か月そろう 91 観測点年すべてが、公表の丸め(0.1 ppm)で一致**。
公表表の `*` 印が有効月数 12 未満を意味することも 4/4 で確認した。

---

## 4. 国立環境研究所 大気汚染常時監視(NIES-AIR / NIES-STN)

| 項目 | 内容 |
|---|---|
| 出典 | 国立環境研究所 環境展望台 大気汚染常時監視データファイル |
| URL | `https://tenbou.nies.go.jp/download/` |
| 取得日 | 2026-09-09 以降 |
| 書式定義 | `TJ_manu.pdf`(時間値)/ `TM_manu.pdf`(測定局) |
| ライセンス | 掲載情報の著作権は国立環境研究所に帰属 |
| 再配布 | **生ファイルは配らない。** 日別に集約した派生値のみ |

### 種別コードは画面の見出しと対応していない(実測で確定)

| 種別 | ファイル名 | 中身 | 年度 |
|---|---|---|---|
| `tm` | `TM{年度}xx{県}.zip` | 測定局マスタ(243 列・度分秒の座標) | 1971–2023 |
| `td` | `TD{年度}{物質}{県}.zip` | 年間値・月間値の**集計**(時系列ではない) | 1971–2023 |
| `tj` | `j{県}_{年度}.zip` | 都道府県内の全測定局の**時間値** | **2009–2023** |
| `tk` | `{年度}.zip` | 国設局 18 局の時間値 | 1976–2023 |

「時間値データ」と題された `td` の中身が集計値であることは、開くまで分からなかった。
**日別値を作れるのは `tj` だけで、2009 年度以降**しかない。

### センチネルは 3 つ(`TJ_manu.pdf`「2. ファイルレイアウト」)

`9999` 欠測値 / `9998` 未測値 / `9997` エラー値。
同書は「測定日 … 31日までない月も31まで入力する」「31日までない月は、9998で埋める」
と定めており、ファイルは **12 か月 × 31 日 = 372 行の固定格子**である。

**`9998` は暦の詰め物専用ではない。**「未測値」一般なので暦にある日にも出る
(実測: 新潟県 FY2014 の 2014-12-01)。当初これを東京 FY2023 の 1 標本から
「暦に無い日にしか出ない」と読んだが、別の標本で崩れた。

### 同じ物質が測定法ごとに別の項目コードで入る(同書コード表 (3))

PM2.5 は `PM25`(β線吸収)/ `PMFL`(フィルター振動)/ `PMBH`(ハイブリッド)の 3 表記で、
いずれも項目番号 12。実測(東京 FY2023)では `PM25` 30,504 行に対し `PMFL` が 1,116 行あった。
**`PM25` だけを拾うと他の測定法の局を黙って落とす。**
浮遊粒子状物質は `SPM`/`SPMP`/`SPMB`、全炭化水素は `THC`/`THCP`/`THCM`。

単位コードには倍率つきのものがある(`0.1PPM` = CO、`0.1'C` = 気温、`0.1M/S` = 風速)。
V1 で使う 4 物質は `PPB`(SO₂・NO₂・Ox)と `UG/M3`(PM2.5)で倍率 1。

### known issues

- 環境展望台は接続を散発的にリセットする。連続取得を続けると頻度が上がるので、
  間隔 5 秒・最大 12 回・待ち上限 300 秒で再試行する。リセットは「データ無し」ではない。
- ブラウザ以外の User-Agent を弾くことがある。
- 検出限界付近では**負の値**が出る(β線吸収法の PM2.5 など)。0 に丸めると平均が
  上に偏るので、そのまま採る。範囲の検査は小さな負値を許す。

---

## 5. 国土地理院 地理院タイル(GSI)

| 項目 | 内容 |
|---|---|
| 出典 | 地理院タイル |
| URL | `https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png` |
| 取得 | 実行時に利用者のブラウザが直接読む(こちらは保存も再配布もしない) |
| ライセンス | 国土地理院コンテンツ利用規約 |
| 表示 | 地図表示中は常時「地理院タイル」を出す |

---

## 収録しなかったもの

| 対象 | 理由 |
|---|---|
| 気象庁 obsdl の一括ダウンロード | 未文書化の内部エンドポイントに依存するため(構想書 §3.2 の禁止) |
| そらまめ君の速報値 | 速報値であり、確定値と混ぜられない。履歴も 7 日程度しかない |
| GOSAT | 利用にユーザー登録と Data Policy の確認が要り、raw を Web に置く設計にしない |
| JRA-3Q | V1 ではアダプタ境界のみ。取得も同梱もしない |
| 2009 年度より前の大気汚染 | 時間値が公開されていない(集計値しかない) |
