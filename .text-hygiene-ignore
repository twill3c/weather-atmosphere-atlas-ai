# 字種・制御文字の検査から外す経路(1 行 1 パターン・部分一致)。
#
# ここに挙げてよいのは「我々が書いた文章ではないもの」だけである。
# 除外を広げると検査が静かに骨抜きになるので、理由を各行に書く。

# 外部データの生の取得物。気象庁・環境展望台の配布物そのもの(2026-09-09 時点で 286 MB)。
# 我々の文章ではないうえ、全走査すると検証段が数分伸びる。
data/cache/

# 取り込みが生成した派生値。人が書いた文は入らない。
data/processed/

# ループログは追記専用の監査証跡で、書き換えてはならない。
logs/loops/
