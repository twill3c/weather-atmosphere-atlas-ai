# SPEC.md — japan-weather-atmosphere-atlas

日本気象・大気環境アトラス AI(Japan Weather & Atmosphere Atlas AI)V1.0

> 本 SPEC は構想書 `Japan_Weather_Atmosphere_Atlas_AI_V1.0_Complete_Implementation_Spec.md`
> を出発点とするが、**構想書の記述と実測が食い違う場合は実測を採る**。
> 食い違いは §11 に、実測日つきで列挙する。
>
> 断定形の文はすべて **実測 / 見込み / 実装の説明** のいずれかに分類できるように書く(HC-077)。
> 実測には値と実測日を添える。見込みには確かめる手段を併記する。

## 1. 目的

日本国内の公開気象・大気環境データ(気象庁の地上気象観測・温室効果ガス、国立環境研究所の
大気汚染常時監視)を、地図と時系列で横断的に見せ、そこに深層学習で作った表現を重ねて
「似た日」「異常度」「パターン」を引けるようにする静的 Web アプリを作る。

**予報はしない。** 気象業務法第 17 条(予報業務の許可)・第 23 条(警報の制限)の制約下にあり、
気象庁の利用規約もそれを前提としている(§10 参照)。本アプリは過去データの可視化・
教育・探索的分析のみを目的とする。

## 2. データ源と実測(2026-09-08 実測)

| ID | データ | 提供元 | 取得経路 | V1 |
|---|---|---|---|---|
| JMA-WX | 地上気象観測 日別値 | 気象庁 | 過去の気象データ検索 `daily_s1.php`(公開の閲覧画面) | 必須 |
| JMA-STN | 観測地点マスタ | 気象庁 | 地点選択ページ内の `viewPoint()` 引数 | 必須 |
| JMA-GHG | 温室効果ガス 月別値 | 気象庁 | 大気・海洋環境観測年報の ZIP 直リンク | 必須 |
| NIES-STN | 測定局マスタ | 国立環境研究所 | 環境展望台 ダウンロード API(種別 `tm`) | 必須 |
| NIES-AIR | 大気汚染 時間値 | 国立環境研究所 | 環境展望台 ダウンロード API(種別 `tj`) | 必須 |
| GSI | 地理院タイル | 国土地理院 | `cyberjapandata.gsi.go.jp/xyz/...` | 必須 |

### 2.1 JMA-WX 地上気象観測(日別値)

- 構想書 §101 が挙げる `/obd/stats/etrn/` は **301 で `/stats/etrn/` へ移動している**(実測)。
- `daily_s1.php?prec_no=&block_no=&year=&month=&day=&view=` が 1 地点 1 か月を返す。
  データ表は `<table id='tablefix1' class='data2_s'>`。文字コードは UTF-8(実測)。
- 構想書 §3.2 は obsdl の未文書化エンドポイントへの依存を禁じている。本 SPEC もこれを守り、
  **obsdl は使わない**。公開の閲覧画面を、間隔を置いて・キャッシュして・再開可能に読む。
- **応答は速い**: 中央値 0.161 s / 平均 0.224 s / 最大 0.474 s(n=6、実測)。
  正常な月ページは 36,846〜37,996 バイト。
- **危険**: `prec_no` と `block_no` の対が不整合でも HTTP 200 と 3,814 バイトの短いページが返る
  (実測 2 例)。状態コードだけを見る取得器はこれを正常として通す → G-02。

#### 値欄の記号(気象庁公式凡例 `stats/data/mdrr/man/remark.html` より・実測で確認)

| 記号 | 意味 | 本アプリでの扱い |
|---|---|---|
| `--` | 該当現象、または該当現象による量等がない | **観測値であって欠測ではない。**降水量・降雪・積雪では 0 とする |
| `0.0` | 量はあるが 0.1 に満たない(降水量は 0.5 mm に満たない) | 観測値 0.0(微量) |
| `)` | 準正常値(資料が許容範囲で欠けている。全体の 80% が基準) | 値は採る。品質 = `quasi_normal` |
| `]` | 資料不足値 | 値は採らない。品質 = `insufficient` |
| `×` | 欠測 | 欠測 |
| `///` | 欠測または未観測 | 欠測 |
| 空白 | 未観測(通信障害・1960 年以前等を含む) | 欠測 |
| `#` | 値にかなり疑問があるため表示していない | 欠測 |

`--` を欠測として扱うと、雨の降らなかった日がすべて消える。**この解釈は §9 の G-01 で検査する。**

### 2.2 JMA-STN 観測地点マスタ

地点選択ページ(`select/prefecture.php?prec_no=NN`)の `viewPoint()` 呼び出しが、
**先頭の 1 件を列名そのものの見出し行として持つ**(実測):

```
as, bk_no, ch, ch_kn, lat_d, lat_m, lon_d, lon_m, height,
f_pre, f_wsp, f_tem, f_sun, f_snc, f_hum, ed_y, ed_m, ed_d, bikou1..bikou5
```

- `as`: `s` = 官署 / `a` = アメダス、`bk_no`: block_no、`lat_m`/`lon_m` は**十進の分**、
  `f_*`: 要素の有無、`ed_y`=`9999` は現役。
- 実測(61 の prec_no を全走査、2026-09-08)。**数える単位で件数が変わるので、母集団を明示する**:

  | 母集団 | 地点 | 官署 | 現役官署 |
  |---|---:|---:|---:|
  | `prec_no` と `bk_no` の対 | 1,679 | 160 | 157 |
  | `block_no` で名寄せした後 | 1,678 | 159 | 156 |
  | さらに国内の矩形で絞った後 | — | — | **155** |

  差の 1 件はいずれも説明がつく: 名寄せで減るのは富士山(下記)、国内で絞って減るのは昭和基地。
  終了地点 391、備考つき 108(対の母集団)。アメダスは 1,519。
- `bk_no=47639`(富士山)だけが 2 つの `prec_no`(49 山梨・50 静岡)に現れる。
  県境の山頂であるため。座標・標高は両者で同一。**block_no で名寄せする** → G-03。
- **「官署」は観測所の種別であって「日本国内の」という限定を持たない。**
  現役官署 156 件には昭和基地(`bk_no=89532`・`prec_no=99`・南緯 68.995 度・東経 39.580 度)が
  入っている。SPEC が対象を国内と書いていても、データ側はその限定を持たない —— 名前が
  含意する限定は述語として書き出して実物に当てる(HC-241)→ G-17。
- 絞り込んだ 155 件の四隅は、独立に知られた極地点と一致する(検算に使う):
  東端・南端 南鳥島(47991、24.288N / 153.983E)、西端 与那国島(47912、123.010E)、
  北端 稚内(47401、45.415N)。**外れ値 1 件は平均や分布には埋もれるが、極値には出る。**
- 現役官署 155 はいずれも気圧・風速・気温・日照・湿度・積雪のフラグをすべて 1 にしている。

### 2.3 JMA-GHG 温室効果ガス

- `env/data/report/data/download/atm_bg/{species}.zip` が直接取得できる(実測:
  co2.zip 11,215,507 B / ch4.zip 9,056,982 B)。
- 観測点は 3 つ: `ry` 綾里 / `mi` 南鳥島 / `yo` 与那国島。
- 月別ファイル `{st}_m.{species}` は空白区切りの固定書式
  `YYYY MM DD RRRRR.RR NNN SS.SSS F`(月別値では `DD`=99)。欠測センチネルは `99999.99`、
  品質フラグ `F` は 3 = 有効・2 = 期間内に有効な時別値なし・9 = 欠測。
- 実測の期間と有効月数:

  | 物質 | 綾里 | 南鳥島 | 与那国島 |
  |---|---|---|---|
  | CO2 | 1987-01..2023-12(444 行 / 有効 443) | 1993-01..(372 / 369) | 1997-01..(324 / 323) |
  | CH4 | 1991-01..2023-12(396 / 358) | 1994-01..(360 / 357) | 1998-01..(312 / 310) |

- **センチネル `99999.99` と `F != 3` は全 2,208 行で完全に一致した**(実測)。
- 添付 readme は **UTF-8**(CP932 ではない)。
- 2023 年に標準ガスのスケールを統一し、観測開始以降の値を最大 +0.3 ppm 程度修正したと
  readme に記載がある。データ版として記録する。

### 2.4 NIES 大気汚染

環境展望台のダウンロード画面自身が使う POST エンドポイント `/download/api/` を、
画面と同じ形で・間隔を置いて呼ぶ。`searchfile.php`(在庫)と `archivedownload.php`(取得)。

**種別コードは画面の見出しと対応していない。実物を開いて確定した**(実測):

| 種別 | ファイル名 | 中身 | 年度 |
|---|---|---|---|
| `tm` | `TM{年度}xx{県}.zip` | **測定局マスタ**(243 列。緯度経度は度・分・秒、標高、設置/廃止、物質別の測定有無) | 1971–2023 |
| `td` | `TD{年度}{物質}{県}.zip` | 年間値・月間値の**集計**(143 列。時系列ではない) | 1971–2023 |
| `tj` | `j{県}_{年度}.zip` | **都道府県内の全測定局の時間値**(`01h`..`24h`) | **2009–2023** |
| `tk` | `{年度}.zip` | 国設局 18 局の時間値 | 1976–2023 |

- 「時間値データ」と題された `td` が実は集計値であることは、**開くまで分からなかった**。
  日別の大気汚染を作れるのは `tj`(2009 年度以降)だけである。
- 実測: `j13_2023.zip` = 7,244,147 B(展開 50,230,256 B・16 ファイル)。
  東京都 FY2023 の測定局は 87。
- 座標は度分秒を十進に直して使う。FY2020 以降は秒に小数を含む局がある
  (東京 FY2023 で 87 局中 2 局)。検算: 国設霞が関 → (35.67278, 139.75306) は
  霞が関の中心から 0.31 km、87/87 が東京都本土部の矩形に入る(実測)。
- **環境展望台は接続を散発的にリセットする**(実測: 数回に 1 回)。
  取得器は再試行を必須とし、リセットを「データ無し」と解釈してはならない。

#### 値の表し方は公式の書式説明書に従う(G-18)

出所は **`TJ_manu.pdf`「大気環境時間値データファイル 利用説明書」**(国立環境研究所
環境情報部 研究情報室。環境展望台のダウンロード画面からリンクされている。2026-09-09 取得)。

| 値 | 意味 |
|---|---|
| `9999` | 欠測値 |
| `9998` | 未測値 |
| `9997` | エラー値 |

同書はレイアウトの備考で「測定日 … 31日までない月も31まで入力する」「31日までない月は、
9998で埋める」と定めている。つまりファイルは **12 か月 × 31 日 = 372 行の固定格子**で、
暦に無い日は `9998` で埋まる。

**ただし `9998` は暦の詰め物専用ではない。**「未測値」一般なので暦にある日にも出る
(実測 2026-09-08: 新潟県 FY2014 の 2014-12-01)。
本プロジェクトは当初、東京 FY2023 の 1 標本から「`9998` は暦に無い日にしか出ない」と
規則を起こしたが、別の標本で崩れた。**外部形式は、標本から規則を起こす前に権威文書を探す。**
3 つのセンチネルはいずれも値として採らず、区別だけ品質に残す。
書式説明書に無い大きい値が出たら例外で止める(黙って欠測に丸めない)。

#### 同じ物質が測定法ごとに別の項目コードで入る(G-18)

同書のコード表 (3) より。**項目番号は同じでも項目コードが分かれる**:

| 物質 | 項目コード | 項目番号 | 単位 |
|---|---|---:|---|
| PM2.5 | `PM25`(β線吸収法) / `PMFL`(フィルター振動法) / `PMBH`(β線吸収・光散乱ハイブリッド法) | 12 | µg/m³ |
| 浮遊粒子状物質 | `SPM` / `SPMP` / `SPMB` | 10 | µg/m³ |
| 全炭化水素 | `THC` / `THCP` / `THCM` | 09 | pphmC |

`PM25` だけを拾うと他の測定法の局を黙って落とす。実測 2026-09-09(東京 FY2023)では
`PM25` 30,504 行に対し `PMFL` が 1,116 行あった。**別名表で束ねる。**

単位コードには**倍率つきのもの**がある(`0.1PPM` = CO、`0.1'C` = 気温、`0.1M/S` = 風速、
`10PPBC` = 炭化水素、`0.01MJ` = 日射量)。V1 で使う 4 物質は `PPB`(SO₂・NO₂・Ox)と
`UG/M3`(PM2.5)で倍率は 1 だが、単位が書式説明書と違えば例外で止める。

### 2.5 権利(実測 2026-09-08)

- **気象庁**: 公共データ利用規約(第 1.0 版)に準拠。出典の記載が必要で、編集・加工した場合は
  その旨を別途記載する。気象業務法第 17 条・第 23 条による制約がある。
- **国土地理院**: 地理院タイルはコンテンツ利用規約に従い、出典「地理院タイル」を地図上に常時表示。
- **国立環境研究所**: 環境展望台の掲載情報の著作権は国立環境研究所に帰属。
  → **生ファイルは再配布しない。** 集計・派生値のみをクレジットつきで公開する(§10)。

## 3. V1.0 の対象範囲

### 3.1 期間と地点(Step 1。構想書 §98 の「いきなり全量を取得しない」に従う)

| 系列 | 地点 | 期間 | 粒度 |
|---|---|---|---|
| 気象 | 国内の現役官署 155 | 2014-01-01 .. 2023-12-31(3,652 日) | 日 |
| 大気汚染 | 47 都道府県の全測定局 | FY2013 .. FY2023 | 時間値 → 日 |
| 温室効果ガス | 3 地点 | 1987-01 .. 2023-12 | 月 |

期間の下限は**大気汚染の時間値が 2009 年度以降しか無い**ことに合わせた(§2.4)。
2014–2023 の 10 年は 3 系列すべてが揃う。

**取得量の実測**(2026-09-10 に完走):

| 系列 | 要求・容量 | 所要 | 結果 |
|---|---|---|---|
| 気象 | 18,588 要求(155 地点 × 10 年 × 12 か月) | 2 並列で 19.0 時間 | 566,060 地点日 |
| 大気汚染 | 470 ファイル・1,110 MB | 断続的に数時間 | 測定局 1,773 |
| 温室効果ガス | 3 要求(ZIP 2 本 + 公表年平均) | 数十秒 | 6 系列 2,208 行 |

**見込みは 2.5 時間だったが、実測は 19.0 時間だった。** 応答時間そのもの
(中央値 0.161 s)は変わっていないので、差は並行して走らせた他の処理との
競合による。**取得の見積もりは応答時間だけからは立たない。**
取得はキャッシュ+再開可能なので、中断しても取得済みぶんは失われない
(実際に 3 度中断して再開した)。

### 3.1.1 配信物の大きさ(実測 2026-09-10)

| 区分 | 容量 | ファイル数 | 最大 |
|---|---:|---:|---:|
| 気象(月別) | 20.6 MB | 120 | 179 KB |
| 大気汚染(物質 × 月) | 82.6 MB | 480 | 238 KB |
| AI | 2.2 MB | 14 | 453 KB(anomaly.json) |
| 温室効果ガス | 0.3 MB | 1 | 262 KB |
| **合計**(地点・manifest を含む) | **105.9 MB** | **617** | **453 KB** |

**1 MB を超えるファイルは 0 件**(N-03)。中央値 172 KB。
大気汚染は物質ごとに分け、**その物質を測っている局だけ**を列に置いている
(全 1,773 局の密な格子にすると大半が null になり数倍に膨らむ)。
似た日は年で割った(1 ファイルにすると 1.4 MB になった)。

### 3.2 明確に対象外

- 独自の天気予報・降水確率予測・台風進路予測・災害警報の発表(気象業務法)
- 個人の健康リスク診断(PM2.5 等からの健康影響の判断)
- リアルタイム防災用途、そらまめ君の速報値の学習利用
- ブラウザ内での大規模学習、Vercel Functions 上の GPU 学習
- GOSAT / JRA-3Q(V1 ではアダプタ境界のみ定義し、取得も同梱もしない)
- 観測地点の無い場所の値を「観測値」として見せること(§6 の補間規律)

## 4. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 地理院タイルを背景に日本地図を表示し、出典を常時表示する | must |
| F-02 | 日付を選ぶとその日の気象観測値が地点マーカーとして地図に出る | must |
| F-03 | レイヤ管理(気象 / 大気汚染 / 温室効果ガス / AI)で表示要素を切り替える | must |
| F-04 | タイムラインの再生・停止で日を送り、Historical Replay ができる | must |
| F-05 | 地点をクリックすると、その日のその地点の全要素と品質・被覆が出る | must |
| F-06 | 大気汚染(PM2.5・NO2・SO2・Ox)を日別値として地図に出す | must |
| F-07 | 温室効果ガスの長期時系列(月別・年平均・季節変動)をグラフで見せる | must |
| F-08 | Autoencoder で日別の埋め込みを作り、静的資産として配る | must |
| F-09 | 選んだ日に似た日を、季節を考慮した既定で上位から出す(自分自身は除く) | must |
| F-10 | 再構成誤差の百分位から異常度 0..100 を出し、AI 由来と明示する | must |
| F-11 | 埋め込みを KMeans でクラスタリングし、シルエットで k を決める | must |
| F-12 | UMAP で 2 次元に落としたパターン地図を出し、点から地図へ飛べる | must |
| F-13 | 2 つの日付を並べて比較し、差分と埋め込み類似度を出す | must |
| F-14 | 観測 / 派生 / AI 由来 を視覚的に区別する(OBSERVED / DERIVED / AI_DERIVED) | must |
| F-15 | データが無い日・要素は「データがありません」と出す(0 と表示しない) | must |
| F-16 | 出典と `/data-policy` を全画面から辿れる | must |
| F-17 | 表示状態(日付・レイヤ・地図位置)を URL で共有できる | should |
| F-18 | 異常度への変数別寄与を「再構成誤差への寄与」として出す | should |
| F-19 | 平年差(気候値からの偏差)を出す | should |
| F-20 | CSV / PNG で書き出す | could |

## 5. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | DB・サーバ関数・有料 API・GPU を常設しない。静的資産のみ | ビルド成果物に API ルートが無いことを検査 |
| N-02 | 初期 JS(gzip)< 500 KB、初期データ < 500 KB、1 レイヤ 1 日 < 1 MB | ビルド後に実測して記録 |
| N-03 | 巨大 JSON を配らない。年月・レイヤで分割する | 配布物の最大ファイルサイズを検査 |
| N-04 | 生の外部データをリポジトリに入れない(NIES の生ファイルは特に) | `.gitignore` と混入検査 |
| N-05 | キーボード操作・aria-label・色以外での異常度伝達・reduced motion 対応 | 実ブラウザ検品 |
| N-06 | 取得は再開可能・キャッシュ済みで、再実行が外部に負荷を掛けない | 2 回目の実行で外部要求 0 件 |

## 6. 科学的レールガード

- 相関を因果として書かない。「気温と CO2 を重ねて見る」は可、「この地域の CO2 が気温を上げた」は不可。
- 補間値を「観測値」と表現しない。補間表示中はその旨を UI に出す。
- 観測点が極端に少ないデータ(温室効果ガス 3 地点)を**面的に補間しない**。
- PM2.5 等から個人の健康影響を診断しない。必要なら公式の環境基準へのリンクを示す。
- AI の出力に根拠のない確信度(「AI confidence 97%」)を付けない。
  出してよいのは入力被覆・再構成誤差・百分位・重心からの距離・類似度。

## 7. データモデル

観測は long format で正規化する。

```
timestamp, station_id, variable, value, unit, quality, source, revision
```

`station_id` は `source` と `source_station_id` から決める(`SHA1(source + ":" + id)[:12]`)。
`quality` は §2.1 の記号と §2.3 のフラグから決め、生の記号は `source_quality_raw` に残す。

データ分類は 3 段階: `OBSERVED`(公式観測値) / `DERIVED`(平均・偏差・補間) /
`AI_DERIVED`(埋め込み・異常度・クラスタ)。

## 8. AI

- 日別特徴量は**気象のみ**から作る。大気汚染は日別値があるので同じ日別軸に載せられるが、
  測定局の分布が気象官署と異なるため、**都道府県単位に集約してから**結合する。
- 温室効果ガスは月別・3 地点であり、日別特徴へ broadcast **しない**
  (構想書 §99 自身の原則)。長期文脈として別画面で扱う。
- 特徴量: 気温平均/最高/最低の平年偏差、降水量 log1p、風速、日照、湿度、
  風向の sin/cos、大気汚染 4 種、月の sin/cos、および**欠測マスク**。
- 欠測は 0 で埋めない。補完値とマスクを連結して入力する。
- 学習・検証・試験は**時系列順に分割**する(ランダム分割を使わない)。
- 損失は欠測を除外した MSE。
- 異常度は再構成誤差の百分位。**災害リスクとは呼ばない。**

### 8.1 異常度は交差適合で作る(G-19、HC-251)

Autoencoder は**学習した期間をよく再構成する**。全期間のモデルの再構成誤差から
素朴に百分位を取ると、その段差がそのまま異常度になる。実測(2026-09-10):

| 期間 | 素朴な百分位 | 交差適合 |
|---|---:|---:|
| 学習(2014–2020)の中央値 | 35.8 | 52.0 |
| 検証(2021–2022)の中央値 | 84.3 | 42.6 |
| 試験(2023)の中央値 | 86.0 | 50.0 |
| **学習と held-out の差** | **+49.0** | **−6.7** |

**同じ特徴量・同じ全期間モデルで両方を計算して比べた**(2026-09-10 実測)。
素朴な方法では、検証期間の **72.9%** の日が「異常度 75 以上」になっていた
(学習期間では 4.1%)。**「2022 年のありふれた日が異常度 84」という嘘**になる。

そこで時間を 5 つの連続ブロックに割り、**各ブロックを、そのブロックを含まない
データで学習したモデルで採点する**。どの日も自分を見ていないモデルに測られるので、
再構成誤差が期間をまたいで比べられる。

**埋め込み(似た日・クラスタ・UMAP)はこの経路を使わない。** あちらは潜在空間が
揃っていることが要るので、全期間で学習した 1 つのモデルを使う。用途が違うので分ける。

### 8.2 この修正を見つけた経緯(記録として残す)

外部知識から選んだ顕著事例 12 日との照合は、**修正前から通っていた**
(事例の異常度の中央値 66.5 対 無作為対照 41.4)。数字だけ見れば
「異常度は顕著な気象を捉えている」と書けた。

期間で割って初めて分かった。事例のうち 4 件が held-out 期間にあり、
それらが 99.7–99.9 という極端な値を出して集計を押し上げていた。
学習期間の事例は 24.9–70.1 で、対照とほとんど変わらない。

**集計した一つの照合は、指標が別物を測っていても通ることがある。**
修正後の照合は、**中央値 94.2 対 対照 30.9**、75 以上が 8/12・90 以上が 7/12。
寄与も物理的に筋が通る(台風の日は風速が 27–47%、豪雨の日は降水量が上位)。

### 8.3 クラスタは弱い(測って、そう書いた)

k を 6 から 16 まで変えてシルエット係数を測った。最大は k=6 の **0.0781** で、
どの k でもほとんど変わらない(**0.0614–0.0781**)。
**日本の日々の気象は、いくつかの型に分かれるというより連続体である。**
クラスタは便宜的な区切りとして出し、画面にもその旨を書く。

### 8.4 既知の限界

特徴量を都道府県平均で全国に均しているので、**地域に集中した極端は薄まる**。
西日本豪雨(2018-07-06)の異常度は **39.4** にとどまった。全国規模の擾乱
(台風・寒波)には強いが、局地的な大雨には弱い。

## 9. 品質ゲート

各ゲートは TEST_SPEC のケースから ID で参照されるか、ここに「未実装」と明記されるかの
いずれかでなければならない(HC-157)。

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | `--` を欠測ではなく 0 として読む。誤って欠測とすると被覆率が 80% を割る月が出ることを対照で示す | 実装済 |
| G-02 | 月ページの取得は、表 `tablefix1` の存在と日付行数が当月日数と一致することを検算し、満たさなければ例外で止める | 実装済 |
| G-03 | 地点は `block_no` で名寄せする。富士山が 2 県に出ることを対照として固定する | 実装済 |
| G-04 | 気象庁公表の CO2 年平均と、配布 ZIP の月別値から自前で出した年平均が、公表の丸め(0.1 ppm)まで一致する | 実装済 |
| G-05 | 日別降水量の合計が公表の月合計と一致し、日別平均気温の月平均が公表の月平均と一致する | 実装済 |
| G-06 | GHG の欠測センチネルと品質フラグが全行で整合する | 実装済 |
| G-07 | NIES 測定局の座標が、その都道府県の外接矩形に入る | 実装済 |
| G-08 | 埋め込みの次元・値域が仕様どおりで、NaN を含まない | 実装済 |
| G-09 | 類似日検索が自分自身を返さない | 実装済 |
| G-10 | 異常度が 0..100 に収まり、同一入力で再現する(seed 固定) | 実装済 |
| G-19 | 異常度の中央値が、学習期間と held-out で 10 点以内に収まる(異常度が『学習したかどうか』を測っていないこと — HC-251) | 実装済 |
| G-11 | 学習・検証・試験の期間が時系列順に並び、重ならない | 実装済 |
| G-12 | 配布 JSON に生の外部ファイルが混入していない | 未実装(L5) |
| G-13 | 欠測を 0 と表示しない(UI) | 未実装(L5) |
| G-14 | 全画面から出典と `/data-policy` に到達できる | 未実装(L5) |
| G-15 | 日本語本文に別字種・制御文字が混入していない(`harness/text_hygiene.py`) | 実装済 |
| G-16 | 図の要素が `viewBox` に収まる / 複数画面幅で横溢れが無い | 未実装(L6) |
| G-17 | 対象地点を国内の矩形で絞り、絞った集合の四隅が独立に知られた極地点と一致する | 実装済 |
| G-18 | NIES 時間値の 2 つのセンチネル(9999=欠測 / 9998=暦の詰め物)の対応が崩れたら例外で止める | 実装済 |

## 10. 出典表示

- 地図上に常時「地理院タイル」。
- データ出典に「気象庁」「国立環境研究所 環境展望台 大気汚染常時監視データ」。
- 加工した旨を明記する(公共データ利用規約の要求)。
- `/data-policy` に、データ源ごとに 出典 / 種別 / 取得日 / 加工の有無 / 再配布の有無 / 条件 URL を載せる。
- 生の NIES ファイルは再配布しない。

## 11. 構想書と実測が食い違った点(2026-09-08)

| 構想書 | 実測 | 対応 |
|---|---|---|
| §101 `/obd/stats/etrn/` | 301 で `/stats/etrn/` へ移動 | 新パスを使う |
| §5.1「時間値データを使う」 | 「時間値」と題する `td` は集計値。時系列は `tj` | `tj` を使う |
| §5.1 大気汚染は長期に使える | 時間値は **2009 年度以降**のみ | 期間の下限を 2014 年に置く |
| §11.2 manifest 例で CO2 が 2026-04 まで | 配布 ZIP の月別値は **2023-12 まで** | 実データから生成する |
| §21/§99 PM2.5 等を日別特徴に入れる | 日別値は作れる(`tj`)。ただし測定局は気象官署と別 | 都道府県に集約してから結合 |
| §19 の画面例(東京 2023-08-05 平均風速 3.2 m/s) | 実測は 4.3 m/s(平均気温 30.4・最高 35.9 は一致) | 画面例は実データで置き換える |

## 12. 開発フェーズ

| ループ | 内容 |
|---|---|
| L0 | データ源の実測、SPEC / TEST_SPEC、骨格、検証済みパーサ |
| L1 | 気象の取得と正規化(地点マスタ + 日別値) |
| L2 | 大気汚染の取得と日別集約 |
| L3 | 温室効果ガスの取り込みと正準データセット + 検証 |
| L4 | 特徴量・Autoencoder・埋め込み・異常度・類似日・クラスタ・UMAP |
| L5 | Web 資産生成と地図 UI |
| L6 | 比較・パターン・出典・実ブラウザ検品 |
| L7 | 最適化・デプロイ・文書 |

## 13. 公開(loop_001・実測 2026-09-14)

| 項目 | 値 |
|---|---|
| 本番 | <https://weather-atmosphere-atlas-ai.vercel.app>(Vercel CLI デプロイ・Git 連携なし) |
| リポジトリ | <https://github.com/twill3c/weather-atmosphere-atlas-ai>(PUBLIC・main) |
| app-menu | 登録簿の planned カード `weather-atmosphere-atlas-ai` をその場で published に上げた |
| 送る木 | 672 ファイル・106.4 MB(`.vercelignore` 適用後。Free の 24 時間 5,000 ファイル上限の内側) |
| ビルド刻印 | `00fb871fe8f92a1c`(638 ファイル: app 14 / components 1 / lib 3 / data 617 / ルート設定 3) |
| 本番検品 | `harness/smoke_prod.mjs` 42 件。**最初に本番の刻印を手元と突き合わせ、一致を確かめてから**残りを見る |

**公開名とローカルのディレクトリ名は違う。** 構想書 §13 はディレクトリを
`japan-weather-atmosphere-atlas` と例示していたが、フリートの登録簿(app-menu)には
同じ題名の planned カードが id `weather-atmosphere-atlas-ai` で先に在った。
先例(jinja-origin-atlas-ai / fishing-port-atlas-ai)は planned をその場で上げて
リポジトリ名を id に合わせているので、公開名はそちらに揃えた。
ローカルのディレクトリは据え置いている(改名は .venv の絶対経路を壊しうる)。
そのため app-menu の `measure_repos.py` はこのプロジェクトを「見つからないディレクトリ」と
報告する —— 揃えるかどうかは利用者の判断に残す。

**反映の判定は刻印で行う。** デプロイの終了コードや `vercel ls` は、上限で失敗したときにも
前の版を「健やか」に見せる。配る木の中身(画面のソースとデータ)から作った刻印を
本番から引き、手元と違えば他を見ずに止める(HC-148)。

**`vercel link` は `.env.local`(OIDC トークン)を作る。** 初回デプロイの時点では
`.vercelignore` が `.env*` を除外していなかった。静的書き出しは `out/` しか配らないので
本番では 404 を確認したが、送る理由も無いので以後は除外する(T-036)。
