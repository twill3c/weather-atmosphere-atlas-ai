# MODEL_CARD

`AtmosphericAutoencoder` v1.0.0 — 日ごとの全国の気象・大気環境を 128 次元の表現に直す
Autoencoder。数値はすべて実測(2026-09-10、完成したデータセットで再測定)。

---

## 目的

日ごとの全国の観測を 1 本のベクトルにまとめ、そこから

- **似た日**(表現どうしのコサイン類似度)
- **異常度**(再構成誤差の百分位)
- **パターン**(クラスタと UMAP の 2 次元配置)

を作る。**予報のためのモデルではない。** 過去の観測を並べ替えて眺めるための道具である。

## 学習データ

| 項目 | 内容 |
|---|---|
| 期間 | 2014-01-01 〜 2023-12-31(3,652 日、欠落なし) |
| 気象 | 国内の現役気象官署 155 地点の日別値(気象庁)= 566,060 地点日 |
| 大気汚染 | 全国 1,773 測定局の時間値を日別に集約(国立環境研究所)= 17,871,330 値 |
| 空間の単位 | 47 都道府県。地点を県で平均してから結合する |
| 観測のある割合 | **0.991**(3,652 × 517 の格子に対して) |

気象官署と大気測定局は分布が違うので、**同じ土俵(都道府県)に載せてから**結合している。
温室効果ガスは月別・3 地点なので、この日別モデルには**入れていない**。

## 特徴量(1,036 次元)

| 区分 | 次元 | 中身 |
|---|---:|---|
| 気象 | 329 | 47 県 × 7 要素(平均/最高/最低気温、降水量、風速、日照、湿度) |
| 大気汚染 | 188 | 47 県 × 4 物質(PM2.5、NO₂、SO₂、Ox) |
| 欠測マスク | 517 | 上と同じ並びで「観測があったか」を 0/1 で |
| 季節 | 2 | 年周の sin / cos |

前処理:

- **季節性を除く。** 気温・日照・湿度は day-of-year の気候値からの偏差(z)にする。
  生の絶対値を入れると、埋め込みは「その日が何月か」を測るだけになる。
- 気候値は前後 7 日の窓で平滑化し、**学習期間だけから**作る(検証・試験を混ぜない)。
- 降水量は `log1p`(裾が長いため)。
- 標準化の統計も学習期間だけから取る。
- **欠測を 0 で埋めない。** 補完値とマスクを連結して渡すので、
  「0 という観測」と「観測が無い」は区別される。

## 構成

```
入力 1,036
 → Dense 512 → ReLU → Dropout 0.1
 → Dense 256 → ReLU
 → Dense 128            ← 埋め込み
 → Dense 256 → ReLU
 → Dense 512 → ReLU
 → 出力 517             ← 値の部分だけを再構成する
```

## 学習

| 項目 | 値 |
|---|---|
| 分割 | 学習 2014-01-01〜2020-12-31(2,557 日)/ 検証 2021-01-01〜2022-12-31(730 日)/ 試験 2023-01-01〜2023-12-31(365 日) |
| 損失 | 欠測を除いた MSE(マスクで重み付けし、マスクの和で割る) |
| 最適化 | Adam, lr 1e-3, batch 64 |
| 打ち切り | 検証が 10 回改善しなければ停止。実際は **61 epoch** で停止 |
| seed | 42 |

**時系列で分ける。** ランダム分割は使わない(隣り合う日が漏れる)。
期間が順に並び重ならないことは実行時に assert している(G-11)。

最終損失: **学習 0.0999 / 検証 0.1709 / 試験 0.1788**。
試験が検証とほぼ同じなので、held-out の性能は安定している。

## 評価

### 異常度は交差適合で作る(G-19 / HC-251)

**同じ特徴量・同じ全期間モデルで、二つの作り方を比べた**(実測 2026-09-10):

| | 素朴(全期間モデルの誤差の百分位) | 交差適合 |
|---|---:|---:|
| 学習(2014–2020)の中央値 | 35.8 | 52.0 |
| 検証(2021–2022)の中央値 | 84.3 | 42.6 |
| 試験(2023)の中央値 | 86.0 | 50.0 |
| **学習と held-out の差** | **+49.0** | **−6.7** |
| 75 以上の割合(学習) | 4.1% | 27.8% |
| 75 以上の割合(検証) | 72.9% | 15.5% |

Autoencoder は学習した期間をよく再構成するので、素朴なやり方では
**「気象の異常」ではなく「その日が学習に使われたか」**を測ってしまう。
検証期間の 72.9% の日が「異常度 75 以上」になり、
**「2022 年のありふれた日が異常度 84」という嘘**を画面に出すことになる。

そこで**時間ブロック 5 分割の交差適合**にした。各ブロックを、そのブロックを
含まないデータで学習したモデルで採点する。どの日も自分を見ていないモデルに
測られるので、再構成誤差が期間をまたいで比べられる。

**埋め込み(似た日・クラスタ・UMAP)はこの経路を使わない。** あちらは潜在空間が
揃っていることが要るので、全期間で学習した 1 つのモデルを使う。用途が違うので分けている。

### 外部の顕著事例との照合(非循環)

モデルは災害のラベルを一切見ていない。気象庁の顕著事例など**外部の知識から選んだ**
12 日の異常度(交差適合):

| | 事例 12 日 | 無作為の対照 12 日 |
|---|---:|---:|
| 中央値 | **94.2** | 30.9 |
| 75 以上 | 8 / 12 | 2 / 12 |
| 90 以上 | 7 / 12 | — |
| 最小 | 39.4(西日本豪雨) | — |

寄与も物理的に筋が通る(台風の日は風速が 27〜47%、豪雨の日は降水量が上位)。

**この照合は、修正前のモデルでも通っていた**(事例の中央値 66.5 対 対照 41.4)。
期間で割ると、held-out 期間の事例だけが 99.7〜99.9 で集計を押し上げ、
学習期間の事例は 24.9〜70.1 と対照と大差なかった。
**集計した一つの照合は、指標が別物を測っていても通る。**

### クラスタは弱い

k を 6 から 16 まで変えてシルエット係数を測った。最大は k=6 の **0.0781** で、
どの k でもほとんど変わらない(**0.0614〜0.0781**)。
**日本の日々の気象は、いくつかの型に分かれるというより連続体である。**
クラスタは便宜的な区切りとして出しており、画面にもそう書いている。

## 限界

- **地域に集中した極端は薄まる。** 特徴量を都道府県平均で全国に均しているので、
  西日本豪雨(2018-07-06)の異常度は **39.4** にとどまった。全国規模の擾乱
  (台風・寒波)には強いが、局地的な大雨には弱い。
- **異常度は災害の危険度ではない。** 「学習した並びからどれだけ外れているか」の順位である。
- **変数別の内訳は因果ではない。** 再構成誤差への寄与であって、原因の説明ではない。
- 大気汚染の時間値は 2009 年度以降しか無いため、期間の下限がそこで決まっている。
- 3 地点しかない温室効果ガスは、この日別モデルに入っていない。

## 不適切な用途

- 天気予報・降水確率・台風進路の予測(**気象業務法第 17 条**)
- 警報・注意報の代替(**同第 23 条**)
- 個人の健康リスクの判断
- リアルタイムの防災判断

## 再現

```bash
bash scripts/rebuild_all.sh      # 検証 → 特徴量 → 学習 → 異常度 → AI 資産 → 配信物
```

個別に回す場合:

```bash
python scripts/ml/build_features.py
python scripts/ml/train_autoencoder.py     # seed 42
python scripts/ml/build_anomaly.py         # 交差適合。G-19 を満たさなければ落ちる
python scripts/ml/build_ai_assets.py
```

`data/models/model_metadata.json` に入力次元・分割・損失・epoch 数・seed を刻んでいる。
`public/data/ai/anomaly.json` には作り方(`method`)と期間ごとの中央値、
学習と held-out の差(`train_holdout_gap`)を入れてある。

## 更新履歴

| 日 | 内容 |
|---|---|
| 2026-09-10 | v1.0.0。大気汚染の全 10 年度がそろったので再学習(観測のある割合 0.897 → 0.991) |
| 2026-09-10 | 異常度を交差適合に変更。素朴な百分位では分割の段差(+49.0 点)を測っていた |
